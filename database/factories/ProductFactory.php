<?php

namespace Database\Factories;

use Illuminate\Database\Eloquent\Factories\Factory;
use Illuminate\Support\Str;

/**
 * @extends \Illuminate\Database\Eloquent\Factories\Factory<\App\Models\Product>
 */
class ProductFactory extends Factory
{
    /**
     * Define the model's default state.
     *
     * @return array<string, mixed>
     */
    public function definition(): array
    {
        return [
//            'name' => fake()->name(),
//            'cat_id' =>fake()->unique()->randomDigitNotNull(),
//            'subcat_id' => fake()->unique()->randomDigitNotNull(),
//            'br_id' => fake()->unique()->randomDigitNotNull(),
//            'unit_id' => fake()->unique()->randomDigitNotNull(),
//            'size_id' => fake()->unique()->randomDigitNotNull(),
//            'color_id' => fake()->numerify(),
//            'code' => fake()->randomDigitNotNull(),
//            'description' => fake()->paragraph(),
//            'price' =>fake()->randomDigitNotNull(),
//            'image' => fake(),
        ];
    }
}
